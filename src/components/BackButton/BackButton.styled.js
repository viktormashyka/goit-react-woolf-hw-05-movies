import styled from 'styled-components';

export const Button = styled.button`
  display: inline-flex;
  width: 100px;
  align-items: center;
  gap: 4px;
  padding: 4px 8px;
  border-radius: 4px;
  color: black;
  font-weight: 500;

  :hover {
    color: orangered;
  }
`;
